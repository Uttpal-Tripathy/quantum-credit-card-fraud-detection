# Quantum-Gated Fraud Detection Architecture (QGFDA)
# Dependency versions verified compatible as of 2026-09-09 (Python 3.11).
# Re-verify with `pip index versions <pkg>` before bumping majors — Qiskit APIs
# have changed across 0.x -> 1.x -> 2.x; this project targets the 2.x API.

# --- Core scientific stack ---
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0
scikit-learn>=1.5,<2.0
joblib>=1.4,<2.0
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0

# --- Classical ML baselines ---
xgboost>=2.1,<4.0
lightgbm>=4.3,<5.0

# --- Imbalanced learning ---
imbalanced-learn>=0.12,<1.0

# --- Explainability ---
shap>=0.46,<1.0

# --- Visualization ---
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
plotly>=5.22,<6.0

# --- Dashboard ---
streamlit>=1.37,<2.0

# --- Live web app (FastAPI backend + HTML5 frontend) ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0

# --- Quantum computing (Qiskit 2.x API) ---
qiskit>=1.3,<3.0
qiskit-machine-learning>=0.8,<1.0
qiskit-aer>=0.15,<1.0
qiskit-algorithms>=0.3,<1.0
qiskit-ibm-runtime>=0.29,<1.0

# --- Testing ---
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# --- CLI / utility ---
tqdm>=4.66,<5.0

# --- Notebooks ---
notebook>=7.0,<8.0
ipykernel>=6.29,<7.0
nbformat>=5.10,<6.0
nbclient>=0.10,<1.0
pylatexenc>=2.10,<3.0
